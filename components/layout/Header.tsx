import RetreatHeader from '@/components/themes/retreat/Header';

/**
 * Site header. Retreat is the only design now — the editorial/modernist/
 * cinematic/monograph variants were removed pre-launch.
 */
const Header = () => <RetreatHeader />;

export default Header;
